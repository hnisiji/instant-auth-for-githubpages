{
  "name": "@instant-lock/action",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "ncc build src/index.ts -o dist && cp -r ../cli/dist/assets dist/",
    "test": "echo \"no test\" && exit 0"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@instant-lock/cli": "^1.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}